[googledrive]
type = drive
client_id = ${GOOGLE_CLIENT_ID}
client_secret = ${GOOGLE_CLIENT_SECRET}
scope = drive
token = ${GOOGLE_TOKEN}
team_drive =

[googledrivecrypt]
type = crypt
remote = googledrive:vault-rc
password = ${GOOGLE_CRYPT_PASSWORD}
